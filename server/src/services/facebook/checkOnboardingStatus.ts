const checkOnboardingStatus = async (wabaId: string) => {
  // check if waba is in db
  // check if waba has cert (completed onboarding)
  // check docusign
  // check if waba has credit line
};

export default checkOnboardingStatus;
